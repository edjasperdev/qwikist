%h1 Hey it's #{current_user.name}
%p Your email is #{current_user.email}
%p You provide #{current_user.service}

-if current_user.provider?
	%h1 Qwikist Certified!!!
	

%h3 Your availabile times:

%ul
	- @appointments.each do |appt|
		%li 
			=link_to appt.time, appointment_path

%h3 Your locations:

%ul
	-@locations.each do |location|
		%li
			=location.name
			%br
			=location.address


= link_to 'Create an Appointment', new_appointment_path
= link_to 'Create a Location', new_location_path
